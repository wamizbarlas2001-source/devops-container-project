apiVersion: v1
kind: Service
metadata:
  name: devops-container-app
spec:
  type: NodePort
  selector:
    app: devops-container-app
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
